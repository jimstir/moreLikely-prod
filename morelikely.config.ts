export const AppConfig = {
  // Toggle this to true to use static offline mock data
  // instead of hitting the live PostgreSQL database and LLM APIs.
  useMockData: false,

  // External marketing site URL. If set, the root path (/) will redirect here.
  marketingWebLink: undefined, // e.g. "https://morelikely.com"
};
